name: build

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Create AppImage from Alpine
        run: |
          cd AppImage/srb2
          make

      - name: Upload artifacts
        uses: actions/upload-artifact@v3
        with:
          name: srt2-appimage
          path: AppImage/srb2/out/*.AppImage
